<UserControl x:Class="Spotnet.Controls.WarningTip" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:controls="clr-namespace:Spotnet.Controls;assembly=Spotnet">
  <ContentControl Name="MainContentControl" Margin="4,4,4,1">
    <FrameworkElement.Style>
      <Style TargetType="{x:Type ContentControl}">
        <Style.Resources>
          <ResourceDictionary>
            <LinearGradientBrush x:Key="ButtonNormalBackground" EndPoint="0,1" StartPoint="0,0">
              <GradientStop Color="#F3F3F3" Offset="0" />
              <GradientStop Color="#EBEBEB" Offset="0.5" />
              <GradientStop Color="#DDDDDD" Offset="0.5" />
              <GradientStop Color="#CDCDCD" Offset="1" />
            </LinearGradientBrush>
            <SolidColorBrush x:Key="ButtonNormalBorder" Color="#FF969696" />
            <Style x:Key="CloseButtonStyle" TargetType="{x:Type Button}">
              <Setter Property="FrameworkElement.FocusVisualStyle" Value="{x:Null}" />
              <Setter Property="Background" Value="{StaticResource ButtonNormalBackground}" />
              <Setter Property="BorderBrush" Value="{StaticResource ButtonNormalBorder}" />
              <Setter Property="BorderThickness" Value="1" />
              <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
              <Setter Property="HorizontalContentAlignment" Value="Center" />
              <Setter Property="VerticalContentAlignment" Value="Center" />
              <Setter Property="Padding" Value="4" />
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="{x:Type Button}">
                    <Grid>
                      <Border Name="Chrome" SnapsToDevicePixels="True" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" CornerRadius="2" Opacity="0.2" />
                      <ContentPresenter SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" Margin="{TemplateBinding Control.Padding}" HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True" />
                    </Grid>
                    <ControlTemplate.Triggers>
                      <Trigger Property="UIElement.IsMouseOver" Value="True">
                        <Setter TargetName="Chrome" Property="UIElement.Opacity" Value="1" />
                        <Setter TargetName="Chrome" Value="{DynamicResource BrightColorBrush3}" Property="Border.Background" />
                      </Trigger>
                      <Trigger Property="IsPressed" Value="True">
                        <Setter TargetName="Chrome" Property="UIElement.Opacity" Value="1" />
                        <Setter TargetName="Chrome" Value="{DynamicResource BrightDarkColorBrush2}" Property="Border.Background" />
                      </Trigger>
                      <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Foreground" Value="#ADADAD" />
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </ResourceDictionary>
        </Style.Resources>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ContentControl}">
              <Grid DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}}">
                <Border Name="Bd" BorderThickness="1" BorderBrush="{DynamicResource NoticeBorderBrush}" Background="{DynamicResource NoticeBackgroundBrush}">
                  <DockPanel Margin="5,5,5,4" Height="Auto" Width="Auto" HorizontalAlignment="Stretch" ClipToBounds="False">
                    <Image HorizontalAlignment="Left" Margin="15,0,5,0" Width="32" Height="32" Source="/Resources/ImagesInternal/info.png" Stretch="Fill" />
                    <TextBlock Name="MessageTextBlock" Margin="5,5,5,5" VerticalAlignment="Center" HorizontalAlignment="Left" TextWrapping="Wrap" Foreground="{DynamicResource NoticeForegroundBrush}" Text="{Binding Text}" />
                    <Button Name="PART_Close" Margin="3,0,3,0" HorizontalAlignment="Right" VerticalAlignment="Top" Visibility="Visible" Width="16" Height="16" Style="{DynamicResource CloseButtonStyle}" Click="CloseMe">
                      <Path Name="Path" Stretch="Fill" StrokeThickness="0.5" Stroke="{DynamicResource NoticeForegroundBrush}" Fill="{DynamicResource NoticeForegroundBrush}" Data="F1 M2.28484E-07,1.33331 L1.33333,0 L4.00001,2.66669 L6.66667,6.10352E-05 L8,1.33331 L5.33334,4 L8,6.66669 L6.66667,8 L4,5.33331 L1.33333,8 L1.086E-07,6.66669 L2.66667,4 L2.28484E-07,1.33331" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                    </Button>
                  </DockPanel>
                </Border>
              </Grid>
              <ControlTemplate.Triggers>
                <Trigger SourceName="PART_Close" Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="Path" Value="{DynamicResource WhiteColorBrush}" Property="Shape.Fill" />
                </Trigger>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="Bd" Value="{DynamicResource BrightColorBrush4}" Property="Border.BorderBrush" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
    </FrameworkElement.Style>
  </ContentControl>
</UserControl>
